<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
  <div class="container-fluid shadowed_header">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navContent">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Stuffs", school_path(@school.name), :class=>'navbar-brand'%>
    </div>

    <div class="right-link-cluster">
      <%= link_to '#', :class=>"right-link" do %>
        <label class="right-link-label">Books</label>
      <% end %>
      <%= link_to '#', :class=>"right-link" do %>
        <label class="right-link-label">Cars</label>
      <% end %>
      <div class="my-dropdown-trigger right-link">
      <% if user_signed_in? %>
        <label href="#" class="right-link-label">
          <%= current_user.name%>
        </label>
        <ul class="my-dropdown-menu">
          <li>
            <%= link_to "My books", school_bookprices_path(@school.name, :status=>0) %>
          </li>
          <li>
            <%= link_to "Settings", edit_user_registration_path%>
          </li>
          <li>
            <%= link_to "Log out", destroy_user_session_path %>
          </li>
        </ul>
      <% else %>
        <label href="#" class="right-link-label">
          Me
        </label>
        <ul class="my-dropdown-menu">
          <li class="rignt-link-dropdown-entry">
              <%= link_to "My books", new_user_session_path, :class=>"right-link-dropdown-a right-link-label"%>
          </li>
          <li class="rignt-link-dropdown-entry">
              <%= link_to "Settings", new_user_session_path, :class=>"right-link-dropdown-a" %>
          </li>
        </ul>
      <% end %>
      </div>
    </div>
  </div>
</nav>

